apiVersion: v1
kind: Service
metadata:
  name: {{ include "architekt.fullname" . }}
  labels:
    {{- include "architekt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "architekt.selectorLabels" . | nindent 4 }}
